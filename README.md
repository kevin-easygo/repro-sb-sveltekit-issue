Run using

```sh
chmod +x ./scripts/setup.sh
./scripts/setup.sh
```

Observe that the story does not match what the app renders.
Then, stop the script with CMD + C and delete `OtherStories.stories.svelte` from the projects.
Notice how the stories now match.